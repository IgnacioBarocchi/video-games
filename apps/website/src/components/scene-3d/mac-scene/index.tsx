import { CustomCanvas } from "../custom-canvas";
import { MacOS3DModel } from "./MacOS3DModel";

export const MacOScene = () => {
  return (
    <CustomCanvas>
      <MacOS3DModel />
    </CustomCanvas>
  );
};
