import { CustomCanvas } from "../custom-canvas";
import { Box3DModel } from "./Box3DModel";

export const BoxScene = () => {
  return (
    <CustomCanvas>
      <Box3DModel />
    </CustomCanvas>
  );
};
