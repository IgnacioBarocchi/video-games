import { CustomCanvas } from "../custom-canvas";
import { Windows3DModel } from "./windows-3d-model";

export const WindowsScene = () => {
  return (
    <CustomCanvas>
      <Windows3DModel />
    </CustomCanvas>
  );
};
