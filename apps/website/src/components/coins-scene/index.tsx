import { CustomCanvas } from "../custom-canvas";
import { Coins3dModel } from "./coins-3d-model";

export const CoinsScene = () => {
  return (
    <CustomCanvas>
      <Coins3dModel />
    </CustomCanvas>
  );
};
